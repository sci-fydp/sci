<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.fydp.sci.grocerything.UserHomeActivity"
    android:descendantFocusability="blocksDescendants">


    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Newlist"
        android:id="@+id/userHome_newListButton"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ReloadList"
        android:id="@+id/userHome_reloadListButton"
        android:layout_below="@id/userHome_newListButton"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Settings"
        android:id="@+id/userHome_settingsButton"
        android:layout_below="@id/userHome_reloadListButton"
        android:layout_centerHorizontal="true" />

    <!--
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="viewlist"
        android:id="@+id/userHome_viewListButton"
        android:layout_below="@id/userHome_newListButton" />
-->
    <ListView

        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/userHome_savedList"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/userHome_settingsButton">
    </ListView>

</RelativeLayout>
